{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Polars-BigQuery","text":"<p>Connect Polars to BigQuery.</p>"},{"location":"#development","title":"Development","text":"<ul> <li>Contributing Overview</li> <li>Development Tasks (<code>maskfile.md</code>)</li> </ul>"},{"location":"development/contributing/","title":"Overview","text":"<p>See the Polars Contributing Guide and the repository Development Tasks (<code>maskfile.md</code>).</p>"},{"location":"development/contributing/maskfile/","title":"Tasks","text":"<p>Development and documentation tasks for <code>polars-bigquery-client</code>.</p> <p>Requires Rust (install with <code>rustup</code>), <code>uv</code>, and <code>mask</code>.</p> <p>For integration and benchmark tests, authenticate with Google Cloud (<code>gcloud auth application-default login</code>) and set the <code>GOOGLE_CLOUD_PROJECT</code> environment variable.</p>"},{"location":"development/contributing/maskfile/#build","title":"build","text":"<p>Build all sub-projects for development.</p> <pre><code>set -euo pipefail\n$MASK rust-arrow-bigquery build\n$MASK py-arrow-bigquery build\n$MASK py-polars-bigquery build\n$MASK docs build\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n&amp; $env:MASK rust-arrow-bigquery build\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-arrow-bigquery build\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-polars-bigquery build\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK docs build\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#rust-arrow-bigquery","title":"rust-arrow-bigquery","text":"<p>Commands for the <code>arrow-bigquery</code> Rust crate (<code>crates/arrow-bigquery</code>).</p>"},{"location":"development/contributing/maskfile/#build_1","title":"build","text":"<p>Compile the Rust workspace and unit test binaries (<code>crates/arrow-bigquery</code>).</p> <pre><code>set -euo pipefail\ncargo test --workspace --exclude py-arrow-bigquery --lib --bins --all-features --no-run\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\ncargo test --workspace --exclude py-arrow-bigquery --lib --bins --all-features --no-run\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#test","title":"test","text":"<p>Run tests for <code>crates/arrow-bigquery</code>.</p>"},{"location":"development/contributing/maskfile/#unit","title":"unit","text":"<p>Run Rust unit tests (<code>crates/arrow-bigquery</code>), matching <code>.github/workflows/test-rust-unit.yml</code>.</p> <pre><code>set -euo pipefail\ncargo test --workspace --exclude py-arrow-bigquery --lib --bins --all-features\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\ncargo test --workspace --exclude py-arrow-bigquery --lib --bins --all-features\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#integration","title":"integration","text":"<p>Run Rust BigQuery integration tests (<code>crates/arrow-bigquery/tests/integration_test.rs</code>). Requires <code>GOOGLE_CLOUD_PROJECT</code>.</p> <pre><code>set -euo pipefail\n: \"${GOOGLE_CLOUD_PROJECT:?Set GOOGLE_CLOUD_PROJECT to run BigQuery integration tests}\"\ncargo test --package arrow-bigquery --test integration_test --all-features\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\nif (-not $env:GOOGLE_CLOUD_PROJECT) {\n  Write-Error \"Set GOOGLE_CLOUD_PROJECT to run BigQuery integration tests\"\n  exit 1\n}\ncargo test --package arrow-bigquery --test integration_test --all-features\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#py-arrow-bigquery","title":"py-arrow-bigquery","text":"<p>Commands for the <code>arrow-bigquery</code> Python package (<code>py-arrow-bigquery</code>).</p>"},{"location":"development/contributing/maskfile/#build_2","title":"build","text":"<p>Build the <code>py-arrow-bigquery</code> Rust extension and install editable dependencies via <code>uv</code>.</p> <p>OPTIONS * resolution     * flags: -r --resolution     * type: string     * desc: Dependency resolution strategy for uv (highest, lowest, lowest-direct) * upgrade     * flags: -U --upgrade     * desc: Allow package upgrades, ignoring pinned versions in uv.lock</p> <pre><code>set -euo pipefail\nuv_args=()\nif [ -n \"${resolution:-}\" ]; then\n  uv_args+=(--resolution \"$resolution\")\nfi\nif [ \"${upgrade:-}\" = \"true\" ]; then\n  uv_args+=(--upgrade)\nfi\nuv sync \"${uv_args[@]}\" --project py-arrow-bigquery --group dev\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$uvArgs = @()\nif ($env:resolution) {\n  $uvArgs += @(\"--resolution\", $env:resolution)\n}\nif ($env:upgrade -eq \"true\") {\n  $uvArgs += \"--upgrade\"\n}\nuv sync @uvArgs --project py-arrow-bigquery --group dev\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#test_1","title":"test","text":"<p>Run tests for <code>py-arrow-bigquery</code>.</p>"},{"location":"development/contributing/maskfile/#unit_1","title":"unit","text":"<p>Run <code>py-arrow-bigquery</code> unit tests, matching <code>.github/workflows/test-py-arrow-unit.yml</code>.</p> <p>OPTIONS * resolution     * flags: -r --resolution     * type: string     * desc: Dependency resolution strategy for uv (highest, lowest, lowest-direct) * upgrade     * flags: -U --upgrade     * desc: Allow package upgrades, ignoring pinned versions in uv.lock</p> <pre><code>set -euo pipefail\nuv_args=()\nif [ -n \"${resolution:-}\" ]; then\n  uv_args+=(--resolution \"$resolution\")\nfi\nif [ \"${upgrade:-}\" = \"true\" ]; then\n  uv_args+=(--upgrade)\nfi\nuv run \"${uv_args[@]}\" --project py-arrow-bigquery --group test pytest -n auto py-arrow-bigquery/tests/unit\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$uvArgs = @()\nif ($env:resolution) {\n  $uvArgs += @(\"--resolution\", $env:resolution)\n}\nif ($env:upgrade -eq \"true\") {\n  $uvArgs += \"--upgrade\"\n}\nuv run @uvArgs --project py-arrow-bigquery --group test pytest -n auto py-arrow-bigquery/tests/unit\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#integration_1","title":"integration","text":"<p>Run <code>py-arrow-bigquery</code> integration tests (if present).</p> <pre><code>set -euo pipefail\necho \"No standalone integration tests in py-arrow-bigquery; covered by rust-arrow-bigquery and py-polars-bigquery.\"\n</code></pre> <pre><code>Write-Host \"No standalone integration tests in py-arrow-bigquery; covered by rust-arrow-bigquery and py-polars-bigquery.\"\n</code></pre>"},{"location":"development/contributing/maskfile/#py-polars-bigquery","title":"py-polars-bigquery","text":"<p>Commands for the <code>polars-bigquery</code> Python package (<code>py-polars-bigquery</code>).</p>"},{"location":"development/contributing/maskfile/#build_3","title":"build","text":"<p>Sync dependencies and install <code>py-polars-bigquery</code> (and workspace member <code>arrow-bigquery</code>) in editable mode via <code>uv</code>.</p> <p>OPTIONS * resolution     * flags: -r --resolution     * type: string     * desc: Dependency resolution strategy for uv (highest, lowest, lowest-direct) * upgrade     * flags: -U --upgrade     * desc: Allow package upgrades, ignoring pinned versions in uv.lock</p> <pre><code>set -euo pipefail\nuv_args=()\nif [ -n \"${resolution:-}\" ]; then\n  uv_args+=(--resolution \"$resolution\")\nfi\nif [ \"${upgrade:-}\" = \"true\" ]; then\n  uv_args+=(--upgrade)\nfi\nuv sync \"${uv_args[@]}\" --project py-polars-bigquery --group dev\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$uvArgs = @()\nif ($env:resolution) {\n  $uvArgs += @(\"--resolution\", $env:resolution)\n}\nif ($env:upgrade -eq \"true\") {\n  $uvArgs += \"--upgrade\"\n}\nuv sync @uvArgs --project py-polars-bigquery --group dev\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#test_2","title":"test","text":"<p>Run tests for <code>py-polars-bigquery</code>.</p>"},{"location":"development/contributing/maskfile/#unit_2","title":"unit","text":"<p>Run <code>py-polars-bigquery</code> unit tests, matching <code>.github/workflows/test-py-polars-unit.yml</code>.</p> <p>OPTIONS * resolution     * flags: -r --resolution     * type: string     * desc: Dependency resolution strategy for uv (highest, lowest, lowest-direct) * upgrade     * flags: -U --upgrade     * desc: Allow package upgrades, ignoring pinned versions in uv.lock</p> <pre><code>set -euo pipefail\nuv_args=()\nif [ -n \"${resolution:-}\" ]; then\n  uv_args+=(--resolution \"$resolution\")\nfi\nif [ \"${upgrade:-}\" = \"true\" ]; then\n  uv_args+=(--upgrade)\nfi\nuv run \"${uv_args[@]}\" --project py-polars-bigquery --group test pytest -n auto py-polars-bigquery/tests/unit\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$uvArgs = @()\nif ($env:resolution) {\n  $uvArgs += @(\"--resolution\", $env:resolution)\n}\nif ($env:upgrade -eq \"true\") {\n  $uvArgs += \"--upgrade\"\n}\nuv run @uvArgs --project py-polars-bigquery --group test pytest -n auto py-polars-bigquery/tests/unit\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#integration_2","title":"integration","text":"<p>Run <code>py-polars-bigquery</code> system/integration tests (<code>py-polars-bigquery/tests/system</code>), matching <code>cloudbuild.yaml</code>. Requires <code>GOOGLE_CLOUD_PROJECT</code>.</p> <pre><code>set -euo pipefail\n: \"${GOOGLE_CLOUD_PROJECT:?Set GOOGLE_CLOUD_PROJECT to run BigQuery integration tests}\"\nuv run --project py-polars-bigquery --group test pytest py-polars-bigquery/tests/system\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\nif (-not $env:GOOGLE_CLOUD_PROJECT) {\n  Write-Error \"Set GOOGLE_CLOUD_PROJECT to run BigQuery integration tests\"\n  exit 1\n}\nuv run --project py-polars-bigquery --group test pytest py-polars-bigquery/tests/system\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#benchmark","title":"benchmark","text":"<p>Run <code>py-polars-bigquery</code> local benchmarks (<code>py-polars-bigquery/tests/benchmark</code>) with <code>pytest-benchmark</code>. Requires <code>GOOGLE_CLOUD_PROJECT</code>.</p> <pre><code>set -euo pipefail\n: \"${GOOGLE_CLOUD_PROJECT:?Set GOOGLE_CLOUD_PROJECT to run BigQuery benchmark tests}\"\nuv run --project py-polars-bigquery --group test pytest --benchmark-only py-polars-bigquery/tests/benchmark\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\nif (-not $env:GOOGLE_CLOUD_PROJECT) {\n  Write-Error \"Set GOOGLE_CLOUD_PROJECT to run BigQuery benchmark tests\"\n  exit 1\n}\nuv run --project py-polars-bigquery --group test pytest --benchmark-only py-polars-bigquery/tests/benchmark\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#docs","title":"docs","text":"<p>Commands relating to the documentation (<code>mkdocs.yml</code>).</p>"},{"location":"development/contributing/maskfile/#build_4","title":"build","text":"<p>Build the static HTML documentation into <code>site/</code> with strict validation enabled.</p> <pre><code>set -euo pipefail\nuv run --with-requirements docs/source/requirements.txt mkdocs build --strict\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\nuv run --with-requirements docs/source/requirements.txt mkdocs build --strict\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#serve","title":"serve","text":"<p>Start a local live-reloading MkDocs development server (default: http://127.0.0.1:8000).</p> <p>OPTIONS * addr     * flags: -a --addr     * type: string     * desc: IP address and port to serve documentation on (default: 127.0.0.1:8000)</p> <pre><code>set -euo pipefail\nuv run --with-requirements docs/source/requirements.txt mkdocs serve --dev-addr \"${addr:-127.0.0.1:8000}\"\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$devAddr = if ($env:addr) { $env:addr } else { \"127.0.0.1:8000\" }\nuv run --with-requirements docs/source/requirements.txt mkdocs serve --dev-addr $devAddr\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#lint","title":"lint","text":"<p>Run ruff lint and formatting checks against the repository and <code>docs/source/</code>.</p> <pre><code>set -euo pipefail\nuv run --project py-polars-bigquery --group dev ruff check --no-fix docs/source/\nuv run --project py-polars-bigquery --group dev ruff format --diff docs/source/\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\nuv run --project py-polars-bigquery --group dev ruff check --no-fix docs/source/\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\nuv run --project py-polars-bigquery --group dev ruff format --diff docs/source/\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#test_3","title":"test","text":"<p>Run project test suites across Rust and Python workspaces.</p>"},{"location":"development/contributing/maskfile/#unit_3","title":"unit","text":"<p>Run all unit tests (<code>rust-arrow-bigquery</code>, <code>py-arrow-bigquery</code>, and <code>py-polars-bigquery</code>).</p> <p>OPTIONS * resolution     * flags: -r --resolution     * type: string     * desc: Dependency resolution strategy for uv (highest, lowest, lowest-direct) * upgrade     * flags: -U --upgrade     * desc: Allow package upgrades, ignoring pinned versions in uv.lock</p> <pre><code>set -euo pipefail\nmask_args=()\nif [ -n \"${resolution:-}\" ]; then\n  mask_args+=(--resolution \"$resolution\")\nfi\nif [ \"${upgrade:-}\" = \"true\" ]; then\n  mask_args+=(--upgrade)\nfi\n$MASK rust-arrow-bigquery test unit\n$MASK py-arrow-bigquery test unit \"${mask_args[@]}\"\n$MASK py-polars-bigquery test unit \"${mask_args[@]}\"\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n$maskArgs = @()\nif ($env:resolution) {\n  $maskArgs += @(\"--resolution\", $env:resolution)\n}\nif ($env:upgrade -eq \"true\") {\n  $maskArgs += \"--upgrade\"\n}\n&amp; $env:MASK rust-arrow-bigquery test unit\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-arrow-bigquery test unit @maskArgs\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-polars-bigquery test unit @maskArgs\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#integration_3","title":"integration","text":"<p>Run all BigQuery integration tests (<code>rust-arrow-bigquery</code>, <code>py-arrow-bigquery</code>, and <code>py-polars-bigquery</code>). Requires <code>GOOGLE_CLOUD_PROJECT</code>.</p> <pre><code>set -euo pipefail\n$MASK rust-arrow-bigquery test integration\n$MASK py-arrow-bigquery test integration\n$MASK py-polars-bigquery test integration\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n&amp; $env:MASK rust-arrow-bigquery test integration\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-arrow-bigquery test integration\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n&amp; $env:MASK py-polars-bigquery test integration\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"},{"location":"development/contributing/maskfile/#benchmark_1","title":"benchmark","text":"<p>Run all BigQuery benchmark tests locally (<code>py-polars-bigquery</code>). Requires <code>GOOGLE_CLOUD_PROJECT</code>.</p> <pre><code>set -euo pipefail\n$MASK py-polars-bigquery test benchmark\n</code></pre> <pre><code>$ErrorActionPreference = \"Stop\"\n&amp; $env:MASK py-polars-bigquery test benchmark\nif ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }\n</code></pre>"}]}